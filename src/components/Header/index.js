import React from 'react';
import Navigation from '../Navigation';

function Header() {
    return(
        <header>
            This is the Header
            <Navigation></Navigation>
        </header>
    );
};

export default Header;